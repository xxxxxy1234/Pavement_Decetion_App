<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name="com.example.pavementdetection.MyApplication"
        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.pavementdetection">

        <meta-data
            android:name="com.amap.api.v2.apikey"
            android:value="0e1c6433b6d6eae88655d076935da44e" />

        <!-- 把原来 HomeActivity 的 intent-filter 移到 LoginActivity -->
        <activity
            android:name=".auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".auth.RegisterActivity" />

        <!-- HomeActivity 去掉 intent-filter，保留 activity 声明即可 -->
        <activity android:name=".home.HomeActivity" />

        <activity android:name="com.example.pavementdetection.detection.MainActivity" />
        <activity android:name="com.example.pavementdetection.settings.SettingsActivity" />
        <activity
            android:name="com.example.pavementdetection.map.MapActivity"
            android:process=":map" />
        <activity android:name="com.example.pavementdetection.history.HistoryActivity" />
        <activity android:name="com.example.pavementdetection.history.EventDetailActivity" />
        <activity android:name=".profile.ProfileActivity" />

    </application>

</manifest>